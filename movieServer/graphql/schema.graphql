type Query {
    getMovies(rating:Int, limit:Float):[movie]!,
    getDetailMovie(movie_id:Int):movie!,
    getSuggestionMovie(movie_id:Int):[movie]!
}

type movie{
    id:Int,
    title:String,
    rating:Float,
    medium_cover_image:String,
    description_full:String,
    genres:[String]
}

# type Mutation{
#     changeAge(changeAgeNum:Int!):Info!
# }
